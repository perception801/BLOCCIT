<div class="media">
  <%= image_tag(comment.user.avatar.small.url, class: 'pull-left')  unless comment.user.nil? %>
  <div class="media-body">
    <small><%= comment.user.name %> commented <%= time_ago_in_words(comment.created_at) %> ago</small>
    <p><%= comment.body %></p>
  </div>
</div>